name: Build and Release Libpostal

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags, e.g. v1.0.0

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf automake libtool pkg-config curl git build-essential

      - name: Clone, Build, and Install libpostal
        run: |
          # Pre-create the data directory with proper permissions
          sudo mkdir -p /usr/local/share/libpostal
          sudo chown $(whoami):$(whoami) /usr/local/share/libpostal
          # Clone libpostal repository and build it
          git clone https://github.com/openvenues/libpostal.git
          cd libpostal
          ./bootstrap.sh
          ./configure
          make -j$(nproc)
          sudo make install
          sudo ldconfig
          # Download libpostal data with sudo so it can create files in /usr/local/share/libpostal
          sudo ./libpostal_data download all /usr/local/share/libpostal

      - name: Create tarball of installed libpostal
        run: |
          # Package the installed libraries, headers, and data into a tarball.
          tar -czvf libpostal-prebuilt.tar.gz \
            /usr/local/lib \
            /usr/local/include \
            /usr/local/share/libpostal || { echo "Tar creation failed"; exit 1; }

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./libpostal-prebuilt.tar.gz
          asset_name: libpostal-prebuilt.tar.gz
          asset_content_type: application/gzip